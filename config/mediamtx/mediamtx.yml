logLevel: info

# HLS Low Latency Configuration
hlsVariant: lowLatency
hlsSegmentCount: 7
hlsSegmentDuration: 2s
hlsPartDuration: 500ms

# Paths (Câmeras)
paths:
  all:
    # Em Dev, podemos permitir publicação simples
    # Em Prod, usaremos runOnDemand com FFmpeg (implementaremos na Sprint 3)